# @joshuadgilley/slopsniff

Node wrapper for the Python `slopsniff` CLI.

## Usage

```bash
npx @joshuadgilley/slopsniff .
```

The wrapper uses `uv` to run the latest `slopsniff` Python tool and will try to install `uv` automatically on macOS/Linux if it is missing.
